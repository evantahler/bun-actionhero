{
  "name": "keryx-example-backend",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "license": "MIT",
  "description": "Keryx example application - backend",
  "bin": {
    "keryx": "keryx.ts"
  },
  "scripts": {
    "start": "bun keryx.ts start",
    "dev": "bun --watch keryx.ts start",
    "migrations": "bun run migrations.ts",
    "lint": "tsc && biome check .",
    "format": "tsc && biome check --write ."
  },
  "dependencies": {
    "keryx": "workspace:*",
    "commander": "^12.1.0",
    "drizzle-orm": "^0.45.1",
    "drizzle-zod": "^0.8.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.9"
  }
}
