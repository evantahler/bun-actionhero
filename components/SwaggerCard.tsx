import { Card } from "react-bootstrap";

export const SwaggerCard = () => {
  return (
    <Card bg="primary">
      <Card.Header>API Endpoints</Card.Header>
      <Card.Body></Card.Body>
    </Card>
  );
};
